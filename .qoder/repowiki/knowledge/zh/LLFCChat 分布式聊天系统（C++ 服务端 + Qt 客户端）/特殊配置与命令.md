必须使用 Ninja Multi-Config 生成器且禁止 in-source build；依赖通过根目录 vcpkg.json 声明并由 cmake/Dependencies.cmake 拉取；集成测试需手动启动 Redis、MySQL 及四个服务端进程后通过 -DLLFC_RUN_INTEGRATION_TESTS=ON 启用；客户端构建会自动复制 config.ini 与 assets/static 到输出目录。