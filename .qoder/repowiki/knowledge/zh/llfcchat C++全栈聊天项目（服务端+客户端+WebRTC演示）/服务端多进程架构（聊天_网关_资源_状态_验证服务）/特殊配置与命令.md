必须使用 Ninja Multi-Config 生成器进行 out-of-source 构建（禁止 in-source build）。根目录 CMakeLists.txt 强制设置 VCPKG_MANIFEST_DIR 和 VCPKG_INSTALLED_DIR 指向仓库根目录的 vcpkg_installed。ChatServer 构建后会额外复制 chatserver2 实例及其配置，支持双实例部署。