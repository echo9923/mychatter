默认构建仅生成 `worker_pool_tests`，可通过 `ctest -R worker_pool_tests` 运行；集成测试需显式开启 `LLFC_RUN_INTEGRATION_TESTS=ON`，要求运行时存在 Redis 与 MySQL 服务，且服务器二进制位于 `${LLFC_REPO_ROOT}/out/run/<CONFIG>/` 下；单个集成场景可通过 `ctest -L integration --test-case im_integration_<scenario>` 单独执行。