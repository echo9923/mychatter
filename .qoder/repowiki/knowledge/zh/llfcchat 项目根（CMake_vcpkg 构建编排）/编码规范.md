- 构建系统强制使用 Ninja Multi-Config 生成器并在 CMakeLists.txt 中进行运行时检查，禁止 in-source build。
- 所有第三方依赖通过 vcpkg 清单文件集中声明，并使用 builtin-baseline 锁定基线版本以保证可重现构建。
- Windows 平台固定使用 x64-windows-static-md triplet 进行静态链接，避免运行时依赖缺失问题。
- 各组件（服务端 Gate/Status/Chat/Resource、客户端 Qt 应用、测试套件）作为独立可执行目标由根 CMake 聚合。