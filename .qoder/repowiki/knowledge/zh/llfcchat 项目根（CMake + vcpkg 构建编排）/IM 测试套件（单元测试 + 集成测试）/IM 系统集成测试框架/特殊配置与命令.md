运行前需确保本地已启动 MySQL（端口 3308，用户 root/密码 123456.，数据库 llfc）与 Redis（端口 6380，密码 123456）；被测服务器可执行文件需位于 ${IM_REPO_ROOT}/out/run/${IM_BUILD_CONFIG}/ 对应子目录（StatusServer.exe、GateServer.exe、ChatServer.exe、ResourceServer.exe）。测试通过 ctest 或自定义 main 入口运行，每个场景在 %TEMP% 下创建 imtest-<pid>-<timestamp> 工作目录并写入 config.ini 与 run.log。