必须在 PowerShell 中先加载 VS BuildTools 环境（`Launch-VsDevShell.ps1 -Arch amd64`），然后执行 `cmake --preset windows-ninja` 配置，再用 `cmake --build out/build/windows-ninja --config Debug|Release --target <目标>` 编译；vcpkg 已安装包位于项目内 `vcpkg_installed/`，无需额外安装。