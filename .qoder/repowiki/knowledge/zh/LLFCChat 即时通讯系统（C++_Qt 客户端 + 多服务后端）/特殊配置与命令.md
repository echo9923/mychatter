必须使用 Ninja Multi-Config 生成器进行配置（根 CMakeLists 会显式检查），且禁止 in-source build；集成测试默认关闭，需设置 `-DLLFC_RUN_INTEGRATION_TESTS=ON` 并在运行前启动 Redis、MySQL 以及各服务端进程；vcpkg 依赖通过根目录 `vcpkg.json` 管理，安装路径固定为 `vcpkg_installed`。