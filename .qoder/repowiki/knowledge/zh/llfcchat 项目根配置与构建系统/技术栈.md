C++17（服务端）+ C++11（Qt 客户端），CMake 3.25+ 配合 Ninja Multi-Config 生成器，vcpkg 清单模式管理依赖，triplet 为 `x64-windows-static-md` 静态链接 MSVC MD 运行时。核心库包括 Boost（asio/beast/filesystem/property-tree/uuid/date-time）、gRPC/Protobuf、Redis（hiredis）、MySQL Connector/C++（JDBC 特性）、Qt5（base + imageformats/webp）。