# 负载均衡

<cite>
**本文引用的文件**   
- [CServer.h](file://server/ChatServer/include/CServer.h)
- [CServer.cpp](file://server/ChatServer/src/CServer.cpp)
- [AsioIOServicePool.h](file://server/GateServer/include/AsioIOServicePool.h)
- [AsioIOServicePool.cpp](file://server/GateServer/src/AsioIOServicePool.cpp)
- [ConfigMgr.h](file://server/ChatServer/include/ConfigMgr.h)
- [ConfigMgr.cpp](file://server/ChatServer/src/ConfigMgr.cpp)
- [StatusServiceImpl.h](file://server/StatusServer/include/StatusServiceImpl.h)
- [StatusServiceImpl.cpp](file://server/StatusServer/src/StatusServiceImpl.cpp)
- [LogicSystem.cpp](file://server/GateServer/src/LogicSystem.cpp)
- [const.h (ChatServer)](file://server/ChatServer/include/const.h)
- [ChatServer.cpp](file://server/ChatServer/src/ChatServer.cpp)
- [day16-asio实现tcp服务器.md](file://开发文档/day16-asio实现tcp服务器.md)
- [day27-分布式服务设计.md](file://开发文档/day27-分布式服务设计.md)
- [day35心跳逻辑.md](file://开发文档/day35心跳逻辑.md)
- [StatusServer CMakeLists.txt](file://server/StatusServer/CMakeLists.txt)
</cite>

## 更新摘要
**变更内容**   
- 将最小连接数算法从基于连接数统计改为基于认证会话计数的最少负载选择
- 新增轮询算法处理负载相等的情况，使用原子计数器避免配置表第一项长期占优
- 引入NoAvailableChatServer错误码（1018）处理无可用节点场景
- 更新负载均衡策略说明，强调认证会话计数作为负载指标
- 完善故障检测与自动恢复流程，包含无可用节点的错误处理

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构总览](#架构总览)
5. [详细组件分析](#详细组件分析)
6. [依赖关系分析](#依赖关系分析)
7. [性能考量与调优](#性能考量与调优)
8. [故障检测与自动恢复](#故障检测与自动恢复)
9. [配置管理](#配置管理)
10. [结论](#结论)
11. [附录：配置模板与示例路径](#附录配置模板与示例路径)

## 简介
本文件面向LLFCChat的负载均衡子系统，围绕以下目标展开：
- 解释负载均衡算法的实现原理（轮询、加权轮询、最少负载选择）
- 说明服务实例发现机制、健康检查与动态扩缩容
- 记录配置管理器的设计模式与动态加载更新能力
- 解析AsioIOServicePool线程池模型与任务分发机制
- 提供策略选择指南、性能调优建议
- 阐述故障检测与自动恢复流程，以及客户端重连机制
- 给出实际代码示例路径与配置模板

**更新** 负载均衡策略已从基于连接数统计的最小连接数算法升级为基于认证会话计数的最少负载选择，并新增轮询算法处理负载相等的情况。

## 项目结构
本项目采用多服务拆分架构，关键服务包括：
- GateServer：HTTP接入层，负责请求接入与转发
- ChatServer：聊天业务TCP服务，维护会话与会话心跳
- StatusServer：状态服务，暴露gRPC接口用于服务注册与健康上报
- ResourceServer：资源服务，处理静态资源等
- VarifyServer：验证码服务（Node.js）

每个服务均包含独立的AsioIOServicePool、CServer、ConfigMgr等基础组件。

```mermaid
graph TB
subgraph "GateServer"
G_CServer["CServer(HTTP接入)"]
G_ASP["AsioIOServicePool(线程池)"]
end
subgraph "ChatServer"
C_CServer["CServer(TCP会话)"]
C_ASP["AsioIOServicePool(线程池)"]
end
subgraph "StatusServer"
S_Server["StatusServiceImpl(gRPC)"]
S_ASP["AsioIOServicePool(线程池)"]
end
subgraph "ResourceServer"
R_CServer["CServer(资源服务)"]
R_ASP["AsioIOServicePool(线程池)"]
end
subgraph "外部存储"
Redis["Redis(会话/计数/锁)"]
MySQL["MySQL(用户/消息)"]
end
G_CServer --> |异步accept| G_ASP
C_CServer --> |异步accept| C_ASP
S_Server --> |gRPC| S_ASP
R_CServer --> |异步accept| R_ASP
C_CServer --> Redis
C_CServer --> MySQL
G_CServer --> S_Server
G_CServer --> C_CServer
```

**图表来源**
- [CServer.cpp:1-133](file://server/ChatServer/src/CServer.cpp#L1-L133)
- [AsioIOServicePool.cpp:1-43](file://server/GateServer/src/AsioIOServicePool.cpp#L1-L43)
- [StatusServer CMakeLists.txt:31-86](file://server/StatusServer/CMakeLists.txt#L31-L86)

章节来源
- [CServer.cpp:1-133](file://server/ChatServer/src/CServer.cpp#L1-L133)
- [AsioIOServicePool.cpp:1-43](file://server/GateServer/src/AsioIOServicePool.cpp#L1-L43)
- [StatusServer CMakeLists.txt:31-86](file://server/StatusServer/CMakeLists.txt#L31-L86)

## 核心组件
- AsioIOServicePool：基于Boost.Asio的IO线程池，使用round-robin分配io_context，避免单点阻塞，提升并发吞吐
- CServer：封装acceptor与session生命周期管理，配合定时器进行心跳检测与过期清理
- ConfigMgr：INI配置文件读取与访问封装，支持按section/key取值
- UserMgr：用户到Session映射管理，支撑踢人、路由等
- RedisMgr/MysqlMgr：缓存与持久化访问封装

**更新** CServer新增GetAuthenticatedSessionCount()方法，用于统计已认证会话数量作为负载指标。

章节来源
- [AsioIOServicePool.h:1-27](file://server/GateServer/include/AsioIOServicePool.h#L1-L27)
- [AsioIOServicePool.cpp:1-43](file://server/GateServer/src/AsioIOServicePool.cpp#L1-L43)
- [CServer.h:1-33](file://server/ChatServer/include/CServer.h#L1-L33)
- [CServer.cpp:1-133](file://server/ChatServer/src/CServer.cpp#L1-L133)
- [ConfigMgr.h:1-84](file://server/ChatServer/include/ConfigMgr.h#L1-L84)
- [ConfigMgr.cpp:1-50](file://server/ChatServer/src/ConfigMgr.cpp#L1-L50)

## 架构总览
整体采用"接入层 + 业务层 + 状态服务"的分层架构：
- 接入层（GateServer）通过HTTP接收请求，调用状态服务获取可用聊天服务列表，再根据负载均衡策略转发至具体ChatServer
- 业务层（ChatServer）维护TCP长连接、会话心跳、用户绑定、消息路由
- 状态服务（StatusServer）对外暴露gRPC接口，供接入层查询服务实例与健康状态

**更新** 负载均衡策略现在基于认证会话计数而非连接数统计，提供更精确的负载衡量。

```mermaid
sequenceDiagram
participant Client as "客户端"
participant Gate as "GateServer(HTTP)"
participant Status as "StatusServer(gRPC)"
participant Chat as "ChatServer(TCP)"
Client->>Gate : HTTP登录/请求
Gate->>Status : 查询可用ChatServer列表
Status->>Status : 读取各ChatServer认证会话数
Status-->>Gate : 返回负载最小的实例(含权重/负载)
Gate->>Gate : 选择策略(轮询/加权/最少负载)
Gate->>Chat : TCP转发或建立新连接
Chat-->>Gate : 业务响应
Gate-->>Client : 返回结果
```

**图表来源**
- [CServer.cpp:1-133](file://server/ChatServer/src/CServer.cpp#L1-L133)
- [StatusServer CMakeLists.txt:31-86](file://server/StatusServer/CMakeLists.txt#L31-L86)

## 详细组件分析

### AsioIOServicePool线程池模型与任务分发
- 设计要点
  - 维护多个boost::asio::io_context实例，每个对应一个工作线程
  - 使用work_guard保证run()不会因无任务退出
  - GetIOService()以轮询方式返回io_context，实现简单而稳定的任务分发
- 复杂度
  - GetIOService()为O(1)，Stop()为O(N)
- 扩展点
  - 可替换为带权重的调度器或基于最小连接数的选择器

```mermaid
classDiagram
class AsioIOServicePool {
+GetIOService() io_context&
+Stop() void
-_ioServices : vector<io_context>
-_works : vector<WorkPtr>
-_threads : vector<thread>
-_nextIOService : size_t
}
```

**图表来源**
- [AsioIOServicePool.h:1-27](file://server/GateServer/include/AsioIOServicePool.h#L1-L27)
- [AsioIOServicePool.cpp:1-43](file://server/GateServer/src/AsioIOServicePool.cpp#L1-L43)

章节来源
- [AsioIOServicePool.h:1-27](file://server/GateServer/include/AsioIOServicePool.h#L1-L27)
- [AsioIOServicePool.cpp:1-43](file://server/GateServer/src/AsioIOServicePool.cpp#L1-L43)
- [day16-asio实现tcp服务器.md:108-167](file://开发文档/day16-asio实现tcp服务器.md#L108-L167)

### CServer会话管理与心跳检测
- 功能要点
  - 异步accept新连接，创建并启动CSession
  - 定时扫描会话，判断心跳是否过期，关闭并清理异常会话
  - 统计当前在线会话数量，写入Redis以便负载均衡决策
- 错误处理
  - 读/写异常时触发DealExceptionSession，释放分布式锁并清理Redis中的会话信息

**更新** 新增GetAuthenticatedSessionCount()方法，仅统计已认证（GetUserId() > 0）的会话，排除尚未登录的连接。

```mermaid
flowchart TD
Start(["定时器回调"]) --> Lock["加锁复制会话表"]
Lock --> Iterate["遍历会话判断心跳过期"]
Iterate --> Expired{"是否过期?"}
Expired --> |是| Close["关闭Socket并收集待清理"]
Expired --> |否| Count["会话计数+1"]
Close --> UpdateRedis["更新Redis中该服务器的在线数"]
Count --> UpdateRedis
UpdateRedis --> Cleanup["逐个清理异常会话(分布式锁→本地锁)"]
Cleanup --> Reschedule["重新设置定时器"]
Reschedule --> End(["结束"])
```

**图表来源**
- [CServer.cpp:75-118](file://server/ChatServer/src/CServer.cpp#L75-L118)
- [day35心跳逻辑.md:318-416](file://开发文档/day35心跳逻辑.md#L318-L416)

章节来源
- [CServer.cpp:1-133](file://server/ChatServer/src/CServer.cpp#L1-L133)
- [day35心跳逻辑.md:318-416](file://开发文档/day35心跳逻辑.md#L318-L416)

### 配置管理器ConfigMgr
- 设计模式
  - 单例模式，进程内唯一配置对象
  - INI文件解析后缓存为map<section, map<key,value>>
- 动态更新
  - 当前实现为启动时加载；可在热更新场景下增加文件监听与原子替换

```mermaid
classDiagram
class SectionInfo {
+_section_datas : map<string,string>
+GetValue(key) string
}
class ConfigMgr {
+Inst() ConfigMgr&
+GetValue(section,key) string
-_config_map : map<string,SectionInfo>
}
ConfigMgr --> SectionInfo : "持有"
```

**图表来源**
- [ConfigMgr.h:1-84](file://server/ChatServer/include/ConfigMgr.h#L1-L84)
- [ConfigMgr.cpp:1-50](file://server/ChatServer/src/ConfigMgr.cpp#L1-L50)

章节来源
- [ConfigMgr.h:1-84](file://server/ChatServer/include/ConfigMgr.h#L1-L84)
- [ConfigMgr.cpp:1-50](file://server/ChatServer/src/ConfigMgr.cpp#L1-L50)

### 负载均衡策略设计与实现
- 策略类型
  - 轮询（Round-Robin）：适合各实例能力相近的场景
  - 加权轮询（Weighted Round-Robin）：按实例权重分配流量
  - 最少负载选择（Least Load）：优先选择当前认证会话最少的实例
- 选择依据
  - 从StatusServer获取实例清单及实时负载指标（如认证会话数、CPU/内存占用）
  - 结合权重与阈值进行过滤与排序
- 实现位置
  - 接入层GateServer在收到请求后，调用状态服务获取实例列表，并在本地执行策略选择

**重大更新** 负载均衡算法已从基于连接数统计的最小连接数算法升级为基于认证会话计数的最少负载选择：

1. **负载指标变更**：从统计所有连接数改为仅统计已认证会话数（GetUserId() > 0）
2. **轮询处理**：当多个节点负载相等时，使用原子计数器进行轮转，避免配置表第一项长期占优
3. **错误处理**：引入NoAvailableChatServer错误码（1018）处理无可用节点场景

```mermaid
flowchart TD
A["收到请求"] --> B["查询StatusServer获取实例清单"]
B --> C["读取各节点chatserver:lease:<name>键值"]
C --> D{"存在有效负载数据?"}
D --> |否| E["返回NoAvailableChatServer(1018)"]
D --> |是| F{"策略选择"}
F --> |轮询| RR["按顺序选取下一个实例"]
F --> |加权轮询| WRR["按权重计算命中概率"]
F --> |最少负载| LC["选择认证会话数最小的实例"]
LC --> G{"负载相等?"}
G --> |是| H["使用原子计数器轮转选择"]
G --> |否| I["直接选择最小负载节点"]
RR --> J["转发/建立连接"]
WRR --> J
H --> J
I --> J
J --> K["返回响应"]
E --> L["客户端重试或降级"]
```

**图表来源**
- [StatusServiceImpl.cpp:111-169](file://server/StatusServer/src/StatusServiceImpl.cpp#L111-L169)
- [LogicSystem.cpp:388-402](file://server/GateServer/src/LogicSystem.cpp#L388-L402)

章节来源
- [StatusServiceImpl.cpp:111-169](file://server/StatusServer/src/StatusServiceImpl.cpp#L111-L169)
- [LogicSystem.cpp:388-402](file://server/GateServer/src/LogicSystem.cpp#L388-L402)
- [day27-分布式服务设计.md:277-444](file://开发文档/day27-分布式服务设计.md#L277-L444)

### 服务实例发现与健康检查
- 实例发现
  - ChatServer启动后向StatusServer注册自身信息（名称、端口、权重等）
  - StatusServer维护实例清单，并提供查询接口
- 健康检查
  - 基于认证会话计数：CServer定时将认证会话数写入Redis，StatusServer据此评估健康度
  - 剔除不健康实例：超过阈值或长时间无心跳的实例将被标记不可用

**更新** 健康检查机制现在使用认证会话计数而非连接数统计，提供更准确的负载反映。

```mermaid
sequenceDiagram
participant Chat as "ChatServer"
participant Status as "StatusServer"
participant Redis as "Redis"
Chat->>Status : 启动时注册(名称/端口/权重)
loop 每5s
Chat->>Redis : SETEX chatserver : lease : <name> <认证会话数>
Status->>Redis : 读取各实例认证会话数
Status-->>Status : 判定健康度(阈值/超时)
end
Status-->>Gate : 返回健康实例清单
```

**图表来源**
- [ChatServer.cpp:50-69](file://server/ChatServer/src/ChatServer.cpp#L50-L69)
- [StatusServiceImpl.cpp:114-143](file://server/StatusServer/src/StatusServiceImpl.cpp#L114-L143)

章节来源
- [ChatServer.cpp:50-69](file://server/ChatServer/src/ChatServer.cpp#L50-L69)
- [StatusServiceImpl.cpp:114-143](file://server/StatusServer/src/StatusServiceImpl.cpp#L114-L143)
- [day27-分布式服务设计.md:277-444](file://开发文档/day27-分布式服务设计.md#L277-L444)

### 动态扩缩容
- 扩容
  - 新增ChatServer实例，完成注册后自动进入健康检查流程
  - 接入层感知到新实例，逐步纳入负载均衡池
- 缩容
  - 下线前主动注销实例，或健康检查将其标记不可用
  - 接入层停止向其派发新请求，等待现有请求完成

章节来源
- [day27-分布式服务设计.md:277-444](file://开发文档/day27-分布式服务设计.md#L277-L444)

## 依赖关系分析
- 组件耦合
  - CServer依赖AsioIOServicePool进行网络IO线程分配
  - CServer依赖ConfigMgr读取服务名等配置
  - CServer依赖RedisMgr进行会话计数与分布式锁
- 外部依赖
  - gRPC用于StatusServer与GateServer通信
  - Redis用于会话计数、分布式锁、实例健康数据
  - MySQL用于用户信息与消息持久化

```mermaid
graph LR
CServer["CServer(ChatServer)"] --> ASP["AsioIOServicePool"]
CServer --> CFG["ConfigMgr"]
CServer --> REDIS["RedisMgr"]
Gate["GateServer"] --> STATUS["StatusServer(gRPC)"]
Gate --> CServer
```

**图表来源**
- [CServer.cpp:1-133](file://server/ChatServer/src/CServer.cpp#L1-L133)
- [AsioIOServicePool.cpp:1-43](file://server/GateServer/src/AsioIOServicePool.cpp#L1-L43)
- [StatusServer CMakeLists.txt:31-86](file://server/StatusServer/CMakeLists.txt#L31-L86)

章节来源
- [CServer.cpp:1-133](file://server/ChatServer/src/CServer.cpp#L1-L133)
- [AsioIOServicePool.cpp:1-43](file://server/GateServer/src/AsioIOServicePool.cpp#L1-L43)
- [StatusServer CMakeLists.txt:31-86](file://server/StatusServer/CMakeLists.txt#L31-L86)

## 性能考量与调优
- IO线程池规模
  - 默认按硬件并发核数初始化，可根据CPU与IO特性调整
  - 高IO密集型可适当增大线程数，但需避免上下文切换开销
- 心跳间隔与超时
  - 建议60秒级别，结合网络质量与业务容忍度调整
  - 过短会增加心跳流量，过长则延迟故障检测
- 负载均衡策略
  - 实例能力相近时使用轮询；差异较大时使用加权轮询
  - 突发流量场景优先最少负载选择，降低拥塞风险
- 缓存与数据库
  - 热点数据尽量走Redis，减少MySQL压力
  - 合理设置TTL与重试退避

**更新** 认证会话计数比连接数统计更能准确反映服务器负载，特别是在连接复用和长连接场景下。

[本节为通用指导，无需特定文件引用]

## 故障检测与自动恢复
- 心跳与过期清理
  - 定时器周期性检测会话心跳，超期即关闭并清理
  - 清理过程遵循"分布式锁→本地锁"的顺序，避免死锁
- 异常会话处理
  - 读/写异常触发DealExceptionSession，释放分布式锁并清理Redis中的会话信息
- 客户端重连
  - 客户端检测到连接断开后，应指数退避重连
  - 接入层对失败实例进行短暂隔离，避免雪崩
- 无可用节点处理
  - 当所有ChatServer的lease缺失或过期时，返回NoAvailableChatServer错误码（1018）
  - 客户端应适当重试或降级处理

**新增** NoAvailableChatServer错误码处理机制，确保系统在有可用节点时才能正常提供服务。

```mermaid
sequenceDiagram
participant Session as "CSession"
participant Server as "CServer"
participant Redis as "Redis"
participant Status as "StatusServer"
Session->>Session : 异步读失败
Session->>Session : Close()
Session->>Server : DealExceptionSession()
Server->>Redis : acquireLock(uid)
alt 成功获得锁
Server->>Redis : 校验会话ID一致性
Server->>Redis : 删除会话与登录信息
Server->>Server : ClearSession(session_id)
Server->>Redis : releaseLock(uid)
else 未获得锁
Server-->>Session : 跳过清理(其他实例处理)
end
Note over Status : 当所有lease缺失时
Status-->>Gate : 返回NoAvailableChatServer(1018)
Gate-->>Client : 错误响应，提示重试
```

**图表来源**
- [day35心跳逻辑.md:318-416](file://开发文档/day35心跳逻辑.md#L318-L416)
- [CServer.cpp:41-73](file://server/ChatServer/src/CServer.cpp#L41-L73)
- [StatusServiceImpl.cpp:44-49](file://server/StatusServer/src/StatusServiceImpl.cpp#L44-L49)

章节来源
- [day35心跳逻辑.md:318-416](file://开发文档/day35心跳逻辑.md#L318-L416)
- [CServer.cpp:41-73](file://server/ChatServer/src/CServer.cpp#L41-L73)
- [StatusServiceImpl.cpp:44-49](file://server/StatusServer/src/StatusServiceImpl.cpp#L44-L49)

## 配置管理
- 配置项建议
  - SelfServer.Name：服务名（用于Redis键区分）
  - Heartbeat.Interval：心跳间隔（秒）
  - MaxConnections：最大连接数阈值
  - LoadBalance.Strategy：负载均衡策略（round_robin/weighted_round_robin/least_load）
  - Discovery.ReportIntervalSeconds：租约上报间隔（默认5秒）
  - Discovery.LeaseTtlSeconds：租约TTL（默认15秒）
  - Redis.Host/Port/DB：Redis连接参数
  - MySQL.Host/Port/User/Pass/DB：数据库连接参数
- 动态更新
  - 当前为启动时加载；可扩展为文件监听+原子替换，避免运行时不一致

**更新** 新增Discovery相关配置项，用于控制认证会话计数的上报频率和租约有效期。

章节来源
- [ConfigMgr.h:1-84](file://server/ChatServer/include/ConfigMgr.h#L1-L84)
- [ConfigMgr.cpp:1-50](file://server/ChatServer/src/ConfigMgr.cpp#L1-L50)

## 结论
本负载均衡方案通过分层架构与明确的职责划分，实现了高并发、可扩展与易运维的目标。AsioIOServicePool提供稳定高效的IO线程池，CServer负责会话与心跳管理，ConfigMgr提供统一配置访问。结合StatusServer的健康检查与实例发现，系统能够动态扩缩容并快速恢复故障。

**重要更新** 新的负载均衡策略基于认证会话计数而非连接数统计，能更准确地反映服务器真实负载。轮询算法确保负载相等时的公平性，NoAvailableChatServer错误码提供了完善的故障处理能力。建议在部署中根据实际负载特征选择合适的负载均衡策略，并持续监控与调优关键参数。

[本节为总结性内容，无需特定文件引用]

## 附录：配置模板与示例路径
- 配置模板（INI）
  - [SelfServer]
    - Name=chat_server_1
  - [Heartbeat]
    - Interval=60
  - [LoadBalance]
    - Strategy=least_load
  - [Discovery]
    - ReportIntervalSeconds=5
    - LeaseTtlSeconds=15
  - [Redis]
    - Host=127.0.0.1
    - Port=6379
    - DB=0
  - [MySQL]
    - Host=127.0.0.1
    - Port=3306
    - User=root
    - Pass=your_password
    - DB=llfc
- 示例路径
  - ChatServer配置读取实现：[ConfigMgr.cpp:1-50](file://server/ChatServer/src/ConfigMgr.cpp#L1-L50)
  - GateServer配置读取实现：[ConfigMgr.cpp:1-50](file://server/GateServer/src/ConfigMgr.cpp#L1-L50)
  - AsioIOServicePool实现：[AsioIOServicePool.cpp:1-43](file://server/GateServer/src/AsioIOServicePool.cpp#L1-L43)
  - CServer心跳与清理：[CServer.cpp:75-118](file://server/ChatServer/src/CServer.cpp#L75-L118)
  - 认证会话计数统计：[CServer.cpp:75-88](file://server/ChatServer/src/CServer.cpp#L75-L88)
  - 负载均衡策略实现：[StatusServiceImpl.cpp:111-169](file://server/StatusServer/src/StatusServiceImpl.cpp#L111-L169)
  - 错误码定义：[const.h (ChatServer):29](file://server/ChatServer/include/const.h#L29)

章节来源
- [ConfigMgr.cpp:1-50](file://server/ChatServer/src/ConfigMgr.cpp#L1-L50)
- [ConfigMgr.cpp:1-50](file://server/GateServer/src/ConfigMgr.cpp#L1-L50)
- [AsioIOServicePool.cpp:1-43](file://server/GateServer/src/AsioIOServicePool.cpp#L1-L43)
- [CServer.cpp:75-118](file://server/ChatServer/src/CServer.cpp#L75-L118)
- [StatusServiceImpl.cpp:111-169](file://server/StatusServer/src/StatusServiceImpl.cpp#L111-L169)
- [const.h (ChatServer):29](file://server/ChatServer/include/const.h#L29)